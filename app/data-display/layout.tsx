import DataDisplayLayout from "../(main)/layouts/DataDisplayLayout"

export default function Layout({
  children,
}: {
  children: React.ReactNode
}) {
  return <DataDisplayLayout>{children}</DataDisplayLayout>
} 